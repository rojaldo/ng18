import { CountryOrderPipe } from './country-order.pipe';

describe('CountryOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
